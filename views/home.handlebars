<nav id="nav" class="navbar navbar-dark bg-dark">
  <!-- Navbar content -->
  <nav class="navbar-brand">
    <button class="btn btn-outline-success" type="button">Home</button>
    <button class="btn btn-outline-info" type="button">Saved Articles</button>
  </nav>
  <button class="btn btn-outline-warning" type="button">Scrape NEW Articles!</button>
</nav>
<section id="title">
  <div class="container">
    <h1>MONGO SCRAPER</h1>
    <p id="rules">Keep up with basketball news. Save articles and add a comment!</p>
    <hr class="my-4">
    <h4>BASKETBALL EDITION<img src="images/ball.jpg" height="35" width="35"></h4>
  </div>
  <div class="col-1">
  </div>

  <section class="section">
    <div class="container">
      {{#if data}}
      {{#each data}}
      {{> scraped}}
      {{/each}}
      {{else}}
      <h1>No Articles Scraped</h1>
      {{/if}}
    </div>
  </section>